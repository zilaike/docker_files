FROM postgres:14.1
ENV SCWS_VERSION 1.2.3
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates wget    bzip2 \
    && apt-get install -y tar make gcc \
    && apt-get postgresql-server-dev-14 \
    &&  mkdir build \
    && cd build \
    &&  wget -q -O - http://www.xunsearch.com/scws/down/scws-$SCWS_VERSION.tar.bz2 | tar xjf -  \
    &&  wget -q -O - https://github.com/amutu/zhparser/archive/master.tar.gz | tar xzf - \
    && cd scws-$SCWS_VERSION && ./configure && make install \
    && rm -rf scws-$SCWS_VERSION \
    && cd zhparser-master && SCWS_HOME=/usr/local make && make install && cd .. \
    && rm -rf zhparser-master \
    && apt-get purge -y --auto-remove ca-certificates wget   bzip2   make gcc
ADD init.sql /docker-entrypoint-initdb.d/